name: obsidian_meta_tool_env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pandas
  - sqlalchemy
  - ruamel.yaml
  # - pyarrow
  # e outros pacotes
  - pip
  - pip:
    - -e .
    - pytest
    - Unidecode
    - tabulate
    #- pytest-cov
    # outros pacotes pip que você usa

